#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>

    using namespace std;

int main()
{
  int n;
    string S;
    cin >> S;
    //auto i=S.begin();
/*     try{
        if (0 < S.length() && S.length() <= 6)
        {
            while (i != S.end())
            {
                
                if (!(*i >= 48 && *i <= 57))
                {
                    throw S;
                }
                i++;
            }
        }
    }
    catch(string S)
    {
        cout<<"Exception Caught";
    }
    
    
    cout<<S;
    */
    return 0;



/* try
{
    n=stoi(S);
    cout<<S;
}
catch(exception e)
{
    cout<<"Bad String";
} */
    n = stoi(S);
    cout << S;
    //In Exeption it directly exits
    return 0;
}
